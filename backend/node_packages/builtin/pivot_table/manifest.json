{
  "id": "vrl.core.pivot_table",
  "name": "Pivot Table",
  "version": "1.0.0",
  "author": "VRL ML Studio",
  "category": "preprocessing",
  "description": "Reshape data into a pivot table with configurable index, columns, values, and aggregation function.",
  "inputs": [
    { "id": "dataframe_in", "type": "DataFrame", "label": "Dataset" }
  ],
  "outputs": [
    { "id": "dataframe_out", "type": "DataFrame", "label": "Pivoted Dataset" }
  ],
  "executor": "executor.py",
  "parameters": "parameters.json",
  "ui": "ui.json",
  "min_studio_version": "1.0.0"
}
